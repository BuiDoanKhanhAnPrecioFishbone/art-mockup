import { Settings } from "lucide-react";
import { PagePlaceholder } from "@/widgets/app-sidebar";

export default function ManageMetadataPage() {
  return <PagePlaceholder title="Manage Metadata" section="Administration" icon={Settings} />;
}
