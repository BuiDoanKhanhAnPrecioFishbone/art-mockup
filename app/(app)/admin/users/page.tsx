import { Settings } from "lucide-react";
import { PagePlaceholder } from "@/widgets/app-sidebar";

export default function ManageUsersPage() {
  return <PagePlaceholder title="Manage Users" section="Administration" icon={Settings} />;
}
