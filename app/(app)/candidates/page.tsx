import { Network } from "lucide-react";
import { PagePlaceholder } from "@/widgets/app-sidebar";

export default function CandidatesPage() {
  return <PagePlaceholder title="Candidates" section="Recruitment & Program" icon={Network} />;
}
